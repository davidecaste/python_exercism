#!/usr/bin/env python3

def directions_to_look(x, y, width, height):
    return[]

def annotate(minefield):
    # Function body starts here
    width = len(minefield[0])
    height = len(minefield)
    return 